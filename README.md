Convert — enter a number in one base (binary/octal/decimal/hex), see it in all four.
Calculate — enter two numbers (each in its own base), pick an operation (+, −, ×, ÷), get the result shown in all four bases.
